name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

# Cancel in-progress runs for the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pr-title-check:
    name: PR Title Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: read
      contents: read
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Check PR title format
      uses: amannn/action-semantic-pull-request@v5
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        types: |
          feat
          fix
          docs
          style
          refactor
          perf
          test
          build
          ci
          chore
          revert
        requireScope: false
        disallowScopes: |
          wip
          temp
        subjectPattern: ^(?![A-Z]).+$
        subjectPatternError: |
          The subject "{subject}" found in the pull request title "{title}"
          didn't match the configured pattern. Please ensure that the subject
          doesn't start with an uppercase character.
        validateSingleCommit: false

  # ============================================================================
  # Build artifacts that can be shared across jobs
  # ============================================================================
  
  build-raydp:
    name: Build RayDP (JARs + Wheel)
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push' }}
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          lib/raydp/**
    
    - name: Skip if no RayDP changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No RayDP files changed, skipping build..."
    
    - name: Set up Java 11
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'
        cache: 'maven'
        cache-dependency-path: 'lib/raydp/java/pom.xml'
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
    
    - name: Build raydp wheel
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd lib/raydp
        # Build wheel (includes JAR compilation via _build_hooks.py)
        uvx --from build pyproject-build --wheel
        echo "Built wheel:"
        ls -la dist/
        # Verify JARs are included
        unzip -l dist/*.whl | grep -E "\.jar$"
    
    - name: Upload wheel artifact
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/upload-artifact@v4
      with:
        name: raydp-wheel
        path: lib/raydp/dist/*.whl
        retention-days: 1

  build-tansu-py:
    name: Build tansu-py Wheel
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push' }}
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          lib/tansu-py/**
    
    - name: Skip if no tansu-py changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No tansu-py files changed, skipping wheel build..."
    
    - name: Set up Rust
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: dtolnay/rust-toolchain@stable
    
    - name: Rust cache
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: Swatinem/rust-cache@v2
      with:
        workspaces: "lib/tansu-py -> target"
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
    
    - name: Build tansu-py wheel
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd lib/tansu-py
        uvx maturin build --release
        echo "Built wheel:"
        ls -la target/wheels/
    
    - name: Upload wheel artifact
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/upload-artifact@v4
      with:
        name: tansu-py-wheel
        path: lib/tansu-py/target/wheels/*.whl
        retention-days: 1

  # ============================================================================
  # Lint and code quality checks
  # ============================================================================

  lint:
    name: Code Quality Check
    runs-on: ubuntu-latest
    needs: [build-tansu-py]
    if: always() && !cancelled()
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          aether/**
          solstice/**
          lib/**
          scripts/**
          pyproject.toml
          uv.lock
    
    - name: Skip if no relevant changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No relevant files changed, skipping..."
    
    - name: Download pre-built wheels
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/download-artifact@v4
      with:
        pattern: '*-wheel'
        path: /tmp/wheels/
        merge-multiple: true
      continue-on-error: true
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: uv python install 3.12
    
    - name: Check license headers
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        python3 scripts/check_license_headers.py
    
    - name: Check aether
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd aether
        uv sync --dev --python 3.12
        uv run --no-sync ruff check .
        uv run --no-sync ruff format --check .

    - name: Check solstice
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        # CI mode: use pre-built wheels via --find-links, skip editable sources
        uv sync --dev --python 3.12 --no-sources --find-links /tmp/wheels/
        uv run --no-sync ruff check solstice/
        uv run --no-sync ruff format --check solstice/
    
    - name: Type check solstice (mypy)
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      # Note: mypy errors are currently warnings only as we gradually add type annotations
      continue-on-error: true
      run: |
        cd solstice
        uv run --no-sync mypy solstice/ || echo "::warning::mypy found type errors (see above)"

  # ============================================================================
  # Aether tests (Python 3.12, no Rust/Java dependencies)
  # ============================================================================

  test-aether:
    name: Aether Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          aether/**
          scripts/**
          pyproject.toml
          uv.lock
    
    - name: Skip if no relevant changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No relevant files changed, skipping..."
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: uv python install 3.12
    
    - name: Install dependencies
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd aether
        uv sync --dev
    
    - name: Run tests
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd aether
        uv run --no-sync pytest tests/ -v
    
    - name: Upload coverage to Codecov
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: codecov/codecov-action@v4
      with:
        file: ./aether/coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  # ============================================================================
  # Solstice unit tests (no external services, fast)
  # ============================================================================

  test-solstice-unit:
    name: Solstice Unit Tests
    runs-on: ubuntu-latest
    needs: [build-raydp, build-tansu-py]
    if: always() && !cancelled()
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          solstice/**
          lib/**
    
    - name: Skip if no Solstice/lib changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No Solstice/lib files changed, skipping..."
    
    - name: Download pre-built wheels
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/download-artifact@v4
      with:
        pattern: '*-wheel'
        path: /tmp/wheels/
        merge-multiple: true
      continue-on-error: true
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: uv python install 3.12
      
    - name: Install dependencies
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        # CI mode: use pre-built wheels via --find-links, skip editable sources
        uv sync --dev --python 3.12 --no-sources --find-links /tmp/wheels/
    
    - name: Run unit tests
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        uv run --no-sync pytest tests/ -v --tb=short -m "not integration and not distributed and not workflow and not chaos and not stability"
    
    - name: Upload Ray logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ray-logs-unit-${{ github.run_id }}
        path: /tmp/ray/
        retention-days: 1
        if-no-files-found: ignore

  # ============================================================================
  # Solstice integration tests (requires Aether, Spark JARs)
  # ============================================================================

  test-solstice-integration:
    name: Solstice Integration Tests
    runs-on: ubuntu-latest
    needs: [build-raydp, build-tansu-py]
    if: always() && !cancelled() && (needs.build-raydp.outputs.should_run == 'true' || needs.build-tansu-py.outputs.should_run == 'true')
    
    steps:
    - name: Free up disk space
      run: |
        echo "Disk space before cleanup:"
        df -h /
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        echo "Disk space after cleanup:"
        df -h /

    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Download pre-built wheels
      uses: actions/download-artifact@v4
      with:
        pattern: '*-wheel'
        path: /tmp/wheels/
        merge-multiple: true
      continue-on-error: true
    
    - name: Set up Java 11
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Start aether services
      run: |
        cd aether
        docker compose build
        docker compose up -d 
        
        echo "Waiting for aether to be ready..."
        for i in {1..30}; do
          if curl -f http://localhost:8000/api/health 2>/dev/null; then
            echo "Aether is ready!"
            break
          fi
          echo "Waiting... ($i/30)"
          sleep 2
        done
        
        docker compose ps
    
    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      run: uv python install 3.12
      
    - name: Install dependencies
      run: |
        cd solstice
        # CI mode: use pre-built wheels via --find-links, skip editable sources
        uv sync --dev --python 3.12 --no-sources --find-links /tmp/wheels/
    
    - name: Run integration tests
      run: |
        cd solstice
        uv run --no-sync pytest tests/ -v --tb=short -m "integration"
    
    - name: Upload Ray logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ray-logs-integration-${{ github.run_id }}
        path: /tmp/ray/
        retention-days: 1
        if-no-files-found: ignore
    
    - name: Stop services
      if: always()
      run: |
        cd aether
        docker compose down -v

  # ============================================================================
  # Solstice distributed tests (multi-worker Ray pipelines)
  # ============================================================================

  test-solstice-distributed:
    name: Solstice Distributed Tests
    runs-on: ubuntu-latest
    needs: [build-raydp, build-tansu-py]
    if: always() && !cancelled()
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          solstice/**
          lib/**
    
    - name: Skip if no Solstice/lib changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No Solstice/lib files changed, skipping..."
    
    - name: Download pre-built wheels
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/download-artifact@v4
      with:
        pattern: '*-wheel'
        path: /tmp/wheels/
        merge-multiple: true
      continue-on-error: true
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: uv python install 3.12
      
    - name: Install dependencies
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        # CI mode: use pre-built wheels via --find-links, skip editable sources
        uv sync --dev --python 3.12 --no-sources --find-links /tmp/wheels/
    
    - name: Run distributed tests
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        uv run --no-sync pytest tests/ -v --tb=short -m "distributed"
    
    - name: Upload Ray logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ray-logs-distributed-${{ github.run_id }}
        path: /tmp/ray/
        retention-days: 1
        if-no-files-found: ignore

  # ============================================================================
  # Solstice stability tests (deterministic fault injection)
  # ============================================================================

  test-solstice-stability:
    name: Solstice Stability Tests
    runs-on: ubuntu-latest
    needs: [build-raydp, build-tansu-py]
    if: always() && !cancelled()
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          solstice/**
          lib/**
    
    - name: Skip if no Solstice/lib changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No Solstice/lib files changed, skipping..."
    
    - name: Download pre-built wheels
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/download-artifact@v4
      with:
        pattern: '*-wheel'
        path: /tmp/wheels/
        merge-multiple: true
      continue-on-error: true
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: uv python install 3.12
      
    - name: Install dependencies
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        # CI mode: use pre-built wheels via --find-links, skip editable sources
        uv sync --dev --python 3.12 --no-sources --find-links /tmp/wheels/
    
    - name: Run stability tests
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        uv run --no-sync pytest tests/ -v --tb=short -m "stability" --timeout=1200
    
    - name: Upload Ray logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ray-logs-stability-${{ github.run_id }}
        path: /tmp/ray/
        retention-days: 1
        if-no-files-found: ignore

  # ============================================================================
  # Solstice workflow tests (end-to-end pipeline tests, slow)
  # ============================================================================

  test-solstice-workflow:
    name: Solstice Workflow Tests
    runs-on: ubuntu-latest
    needs: [build-raydp, build-tansu-py]
    if: always() && !cancelled()
    # Workflow tests are slow - doesn't block PR merge
    continue-on-error: true
    
    steps:
    - name: Free up disk space
      run: |
        echo "Disk space before cleanup:"
        df -h /
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/ghc
        sudo rm -rf /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        echo "Disk space after cleanup:"
        df -h /

    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          solstice/**
          lib/**
    
    - name: Skip if no Solstice/lib changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No Solstice/lib files changed, skipping..."
    
    - name: Install system dependencies
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg
    
    - name: Download pre-built wheels
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/download-artifact@v4
      with:
        pattern: '*-wheel'
        path: /tmp/wheels/
        merge-multiple: true
      continue-on-error: true
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: uv python install 3.12
      
    - name: Install dependencies
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        # CI mode: use pre-built wheels via --find-links, skip editable sources
        uv sync --dev --python 3.12 --no-sources --find-links /tmp/wheels/
    
    - name: Run workflow tests
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        uv run --no-sync pytest tests/ -v --tb=short -m "workflow" --timeout=1200
    
    - name: Upload Ray logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ray-logs-workflow-${{ github.run_id }}
        path: /tmp/ray/
        retention-days: 1
        if-no-files-found: ignore
    
    - name: Cleanup test artifacts
      if: always()
      run: |
        rm -rf /tmp/video_workflow_test_*
        rm -rf /tmp/minhash_*
        rm -rf /tmp/solstice_cache

  # ============================================================================
  # Solstice chaos tests (experimental, unstable)
  # ============================================================================

  test-solstice-chaos:
    name: Solstice Chaos Tests
    runs-on: ubuntu-latest
    needs: [build-raydp, build-tansu-py]
    if: always() && !cancelled()
    # Chaos tests are experimental and may be flaky - doesn't block PR merge
    continue-on-error: true
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v45
      with:
        files: |
          solstice/**
          lib/**
    
    - name: Skip if no Solstice/lib changes
      if: steps.changed-files.outputs.any_changed == 'false' && github.event_name == 'pull_request'
      run: echo "No Solstice/lib files changed, skipping..."
    
    - name: Download pre-built wheels
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: actions/download-artifact@v4
      with:
        pattern: '*-wheel'
        path: /tmp/wheels/
        merge-multiple: true
      continue-on-error: true
    
    - name: Install uv
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python 3.12
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: uv python install 3.12
      
    - name: Install dependencies
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        # CI mode: use pre-built wheels via --find-links, skip editable sources
        uv sync --dev --python 3.12 --no-sources --find-links /tmp/wheels/

    - name: Run chaos tests
      if: steps.changed-files.outputs.any_changed == 'true' || github.event_name == 'push'
      run: |
        cd solstice
        uv run --no-sync pytest tests/ -v --tb=short -m "chaos" --timeout=600
    
    - name: Upload Ray logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ray-logs-chaos-${{ github.run_id }}
        path: /tmp/ray/
        retention-days: 1
        if-no-files-found: ignore
